{% extends 'base.html.twig' %}

{% block title %}Edit Path{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Path</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('main_path') }}">
                    View All Pathways
                </a>
            </div>
            <div class="card-body">
                {% include 'path/_form.html.twig' with {'button_label': 'Update Pathway', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}